/**
 * features/user — company-admin user management (list, invite, detail). Standard slice layout:
 * columns · components/.
 */
export { userColumns, type UserRow } from "./columns";
export { InviteEmployeeDialog } from "./components/invite-employee-dialog";
